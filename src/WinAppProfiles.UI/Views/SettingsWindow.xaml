<Window x:Class="WinAppProfiles.UI.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:WinAppProfiles.UI.ViewModels"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:models="clr-namespace:WinAppProfiles.Core.Models;assembly=WinAppProfiles.Core"
        mc:Ignorable="d"
        Title="Settings" Height="450" Width="400"
        Background="{DynamicResource Theme.WindowBackgroundBrush}"
        Foreground="{DynamicResource Theme.ForegroundBrush}"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource Theme.ForegroundBrush}" />
        </Style>
        <ObjectDataProvider x:Key="ProfileIdValues" MethodName="GetProfilesAsync" ObjectType="{x:Type vm:SettingsViewModel}" />
        <ObjectDataProvider x:Key="DesiredStateValues" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="models:DesiredState" />
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Default Profile:" VerticalAlignment="Center" Margin="0,0,10,5" />
        <ComboBox Grid.Row="0" Grid.Column="1"
                  ItemsSource="{Binding AvailableProfiles}"
                  DisplayMemberPath="Name"
                  SelectedValuePath="Id"
                  SelectedValue="{Binding DefaultProfileId}"
                  Margin="0,0,0,5" />

        <CheckBox Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                  Content="Automatically apply default profile on launch"
                  IsChecked="{Binding AutoApplyDefaultProfile}"
                  Margin="0,5" />

        <CheckBox Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                  Content="Enable dark mode on launch"
                  IsChecked="{Binding EnableDarkMode}"
                  Margin="0,5" />

        <CheckBox Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2"
                  Content="Minimize to system tray on launch"
                  IsChecked="{Binding MinimizeOnLaunch}"
                  Margin="0,5" />

        <CheckBox Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2"
                  Content="Minimize to system tray instead of closing main window"
                  IsChecked="{Binding MinimizeToTrayOnClose}"
                  Margin="0,5" />

        <StackPanel Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Save" Command="{Binding SaveCommand}" Width="80" Margin="0,0,10,0" />
            <Button Content="Cancel" Command="{Binding CancelCommand}" Width="80" />
        </StackPanel>
    </Grid>
</Window>
