<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:vm="clr-namespace:WinAppProfiles.UI.ViewModels">

    <vm:BooleanToVisibilityConverter x:Key="BoolToVis"/>

    <!-- Color Palette - Matching Mock -->
    <SolidColorBrush x:Key="BackgroundPrimary" Color="#121212"/><!-- Window background (darkest) -->
    <SolidColorBrush x:Key="BackgroundSecondary" Color="#1E1E1E"/><!-- Panel background -->
    <SolidColorBrush x:Key="BackgroundTertiary" Color="#252525"/><!-- Search box background -->
    <SolidColorBrush x:Key="BackgroundQuaternary" Color="#2B2B2B"/><!-- Card background (lightest) -->

    <SolidColorBrush x:Key="TextPrimary" Color="#E0E0E0"/>
    <SolidColorBrush x:Key="TextSecondary" Color="#B0B0B0"/>
    <SolidColorBrush x:Key="TextMuted" Color="#808080"/>

    <SolidColorBrush x:Key="AccentPrimary" Color="#0078D4"/>
    <SolidColorBrush x:Key="AccentHover" Color="#1084D8"/>
    <SolidColorBrush x:Key="AccentPressed" Color="#006CBE"/>

    <SolidColorBrush x:Key="StatusRunning" Color="#28A745"/>
    <SolidColorBrush x:Key="StatusStopped" Color="#DC3545"/>
    <SolidColorBrush x:Key="StatusError" Color="#DC3545"/>
    <SolidColorBrush x:Key="StatusNotFound" Color="#6C757D"/>
    <SolidColorBrush x:Key="StatusUnknown" Color="#FFC107"/>

    <SolidColorBrush x:Key="IconPlaceholder" Color="#555555"/>
    <SolidColorBrush x:Key="ToggleOff" Color="#3E3E42"/>
    <SolidColorBrush x:Key="ToggleOffKnob" Color="#B0B0B0"/>

    <!-- Typography -->
    <Style x:Key="HeaderText" TargetType="TextBlock">
        <Setter Property="FontSize" Value="24"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
    </Style>

    <Style x:Key="SectionHeaderText" TargetType="TextBlock">
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
        <Setter Property="Margin" Value="0,0,0,15"/>
    </Style>

    <Style x:Key="CardTitleText" TargetType="TextBlock">
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
        <Setter Property="TextAlignment" Value="Center"/>
        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
    </Style>

    <Style x:Key="StatusText" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
    </Style>

    <!-- Toggle Switch Style -->
    <Style x:Key="ToggleSwitchStyle" TargetType="CheckBox">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <Grid Cursor="Hand">
                        <Border x:Name="Border"
                                CornerRadius="10"
                                Background="{StaticResource ToggleOff}"
                                Width="40"
                                Height="20">
                            <Ellipse x:Name="Ellipse"
                                     Fill="{StaticResource ToggleOffKnob}"
                                     Width="16"
                                     Height="16"
                                     HorizontalAlignment="Left"
                                     Margin="2"/>
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource AccentPrimary}"/>
                            <Setter TargetName="Ellipse" Property="HorizontalAlignment" Value="Right"/>
                            <Setter TargetName="Ellipse" Property="Fill" Value="White"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Opacity" Value="0.9"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Profile Card Template -->
    <DataTemplate x:Key="ProfileItemCardTemplate">
        <Border x:Name="CardBorder"
                Background="{StaticResource BackgroundQuaternary}"
                CornerRadius="8"
                Width="180"
                Margin="0,0,15,0"
                Padding="15">
            <Border.Effect>
                <DropShadowEffect Color="Black"
                                  Opacity="0.6"
                                  ShadowDepth="4"
                                  BlurRadius="15"
                                  Direction="270"
                                  RenderingBias="Quality"/>
            </Border.Effect>
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Height" Value="200"/>
                    <Setter Property="Opacity" Value="1"/>
                    <Style.Triggers>
                        <!-- Dim the card if the item doesn't exist -->
                        <DataTrigger Binding="{Binding Exists}" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </DataTrigger>
                        <!-- Set Tag when mouse is over the card -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Tag" Value="True"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <Grid>
                <StackPanel>
                    <!-- Application Icon -->
                    <Border Width="64"
                            Height="64"
                            Background="Transparent"
                            CornerRadius="4"
                            HorizontalAlignment="Center"
                            Margin="0,0,0,15">
                        <Image Source="{Binding Icon}"
                               Width="64"
                               Height="64"
                               Stretch="Uniform"
                               RenderOptions.BitmapScalingMode="HighQuality"/>
                    </Border>

                    <!-- Application Name -->
                    <TextBlock Text="{Binding DisplayName}"
                               Style="{StaticResource CardTitleText}"
                               Margin="0,0,0,10"/>

                    <!-- Status Indicator -->
                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Center"
                                Margin="0,0,0,15">
                        <Ellipse Width="10"
                                 Height="10"
                                 Margin="0,0,8,0">
                            <Ellipse.Style>
                                <Style TargetType="Ellipse">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentState}" Value="Running">
                                            <Setter Property="Fill" Value="{StaticResource StatusRunning}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding CurrentState}" Value="Not Running">
                                            <Setter Property="Fill" Value="{StaticResource StatusStopped}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding CurrentState}" Value="Stopped">
                                            <Setter Property="Fill" Value="{StaticResource StatusStopped}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding CurrentState}" Value="Unknown">
                                            <Setter Property="Fill" Value="{StaticResource StatusUnknown}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding CurrentState}" Value="Not Found">
                                            <Setter Property="Fill" Value="{StaticResource StatusNotFound}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding CurrentState}" Value="Error">
                                            <Setter Property="Fill" Value="{StaticResource StatusError}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Ellipse.Style>
                        </Ellipse>
                        <TextBlock Text="{Binding CurrentState}"
                                   Style="{StaticResource StatusText}"/>
                    </StackPanel>

                    <!-- Toggle Switch -->
                    <CheckBox x:Name="ToggleSwitch"
                              IsChecked="{Binding IsDesiredRunning, Mode=TwoWay}"
                              HorizontalAlignment="Center"
                              ToolTip="Toggle to set desired state: ON = Start when profile is applied, OFF = Stop when profile is applied">
                        <CheckBox.Style>
                            <Style TargetType="CheckBox" BasedOn="{StaticResource ToggleSwitchStyle}">
                                <Style.Triggers>
                                    <!-- Disable toggle switch if item doesn't exist -->
                                    <DataTrigger Binding="{Binding Exists}" Value="False">
                                        <Setter Property="IsEnabled" Value="False"/>
                                        <Setter Property="ToolTip" Value="This item was not found on the system"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </CheckBox.Style>
                    </CheckBox>
                </StackPanel>

                <!-- Gear Icon - Top Right Corner (Visible on hover) -->
                <Button Content="⚙️"
                        Background="Transparent"
                        BorderThickness="0"
                        FontSize="16"
                        Foreground="{StaticResource TextSecondary}"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Margin="0,-5,-5,0"
                        Padding="5"
                        Cursor="Hand"
                        ToolTip="Edit item settings"
                        Command="{Binding DataContext.OpenItemSettingsCommand, RelativeSource={RelativeSource AncestorType={x:Type Window}}}"
                        CommandParameter="{Binding}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Opacity" Value="0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                            VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource AncestorType=Border}}" Value="True">
                                    <Setter Property="Opacity" Value="0.8"/>
                                </DataTrigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Opacity" Value="1"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Border>
    </DataTemplate>

    <!-- Needs Review Card Template -->
    <DataTemplate x:Key="NeedsReviewCardTemplate">
        <Border Background="{StaticResource BackgroundQuaternary}"
                CornerRadius="8"
                Width="180"
                Height="200"
                Margin="0,0,15,0"
                Padding="15">
            <Border.Effect>
                <DropShadowEffect Color="Black"
                                  Opacity="0.6"
                                  ShadowDepth="4"
                                  BlurRadius="15"
                                  Direction="270"
                                  RenderingBias="Quality"/>
            </Border.Effect>
            <StackPanel>
                <!-- Application Icon -->
                <Border Width="64"
                        Height="64"
                        Background="Transparent"
                        CornerRadius="4"
                        HorizontalAlignment="Center"
                        Margin="0,0,0,15">
                    <Image Source="{Binding Icon}"
                           Width="64"
                           Height="64"
                           Stretch="Uniform"
                           RenderOptions.BitmapScalingMode="HighQuality"/>
                </Border>

                <!-- Application Name -->
                <TextBlock Text="{Binding DisplayName}"
                           Style="{StaticResource CardTitleText}"
                           Margin="0,0,0,25"/>

                <!-- Add to Profile Button -->
                <Button Content="Add to Profile"
                        Background="{StaticResource BackgroundTertiary}"
                        BorderThickness="0"
                        Foreground="White"
                        FontSize="12"
                        Padding="10,6"
                        Cursor="Hand"
                        Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type Window}}, Path=DataContext.PromoteNeedsReviewItemCommand}"
                        CommandParameter="{Binding}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="4"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                            VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#4E4E52"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="#2E2E32"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Border>
    </DataTemplate>

    <!-- Primary Button Style -->
    <Style x:Key="PrimaryButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource AccentPrimary}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="20,10"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            CornerRadius="4"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center"
                                        VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource AccentHover}"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{StaticResource AccentPressed}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.5"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Secondary Button Style -->
    <Style x:Key="SecondaryButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource BackgroundTertiary}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="15,10"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            CornerRadius="4"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center"
                                        VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#4E4E52"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#2E2E32"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.5"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Danger Button Style (remove/delete actions) -->
    <Style x:Key="DangerButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="#C0392B"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="15,8"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            CornerRadius="4"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#E74C3C"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#A93226"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.5"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Search Box Style -->
    <Style x:Key="SearchBoxStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource BackgroundTertiary}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="CornerRadius" Value="20"/>
        <Setter Property="Padding" Value="15,10"/>
        <Setter Property="Margin" Value="0,0,0,10"/>
    </Style>

    <Style x:Key="SearchTextBoxStyle" TargetType="TextBox">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="CaretBrush" Value="{StaticResource TextPrimary}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border Background="{TemplateBinding Background}"
                            BorderThickness="0"
                            BorderBrush="Transparent">
                        <ScrollViewer x:Name="PART_ContentHost"
                                    Focusable="false"
                                    HorizontalScrollBarVisibility="Hidden"
                                    VerticalScrollBarVisibility="Hidden"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ComboBox Style -->
    <Style x:Key="ProfileComboBoxStyle" TargetType="ComboBox">
        <Setter Property="Background" Value="{StaticResource BackgroundTertiary}"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Padding" Value="10,8"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton x:Name="ToggleButton"
                                    Background="{TemplateBinding Background}"
                                    BorderThickness="0"
                                    Focusable="false"
                                    IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                    ClickMode="Press">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="4"
                                            Padding="10,8">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="20"/>
                                            </Grid.ColumnDefinitions>
                                            <ContentPresenter Grid.Column="0"
                                                            HorizontalAlignment="Left"
                                                            VerticalAlignment="Center"/>
                                            <Path Grid.Column="1"
                                                Data="M 0 0 L 4 4 L 8 0 Z"
                                                Fill="{StaticResource TextSecondary}"
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center"/>
                                        </Grid>
                                    </Border>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>
                        <ContentPresenter x:Name="ContentSite"
                                        IsHitTestVisible="False"
                                        Content="{TemplateBinding SelectionBoxItem}"
                                        ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                        ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                        Margin="10,8"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Left"/>
                        <Popup x:Name="Popup"
                             Placement="Bottom"
                             IsOpen="{TemplateBinding IsDropDownOpen}"
                             AllowsTransparency="True"
                             Focusable="False"
                             PopupAnimation="Slide">
                            <Grid x:Name="DropDown"
                                SnapsToDevicePixels="True"
                                MinWidth="{TemplateBinding ActualWidth}"
                                MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                <Border x:Name="DropDownBorder"
                                      Background="{StaticResource BackgroundTertiary}"
                                      BorderThickness="0"
                                      CornerRadius="4"
                                      Margin="0,2,0,0">
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Grid>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#4E4E52"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ComboBox Item Style -->
    <Style x:Key="ProfileComboBoxItemStyle" TargetType="ComboBoxItem">
        <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Padding" Value="10,8"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter Property="Background" Value="{StaticResource AccentPrimary}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource AccentPrimary}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ListView Item Container Style - Centers cards and handles selection -->
    <Style x:Key="CardListViewItemStyle" TargetType="ListViewItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Margin" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListViewItem">
                    <Border Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center"
                                        VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#1A0078D4"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#0A0078D4"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ListView Style -->
    <Style x:Key="CardListViewStyle" TargetType="ListView">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Disabled"/>
        <Setter Property="Margin" Value="0,0,0,30"/>
    </Style>

    <!-- Scrollbar Styles - Thin, barely visible like mock -->
    <Style TargetType="{x:Type ScrollBar}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Stylus.IsPressAndHoldEnabled" Value="false"/>
        <Setter Property="Stylus.IsFlicksEnabled" Value="false"/>
        <Setter Property="Width" Value="4"/>
        <Setter Property="MinWidth" Value="4"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ScrollBar}">
                    <Grid x:Name="Bg" SnapsToDevicePixels="true" Background="Transparent">
                        <Track x:Name="PART_Track" IsDirectionReversed="true">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Opacity="0" Command="ScrollBar.PageUpCommand"/>
                            </Track.DecreaseRepeatButton>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Opacity="0" Command="ScrollBar.PageDownCommand"/>
                            </Track.IncreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb>
                                    <Thumb.Template>
                                        <ControlTemplate TargetType="{x:Type Thumb}">
                                            <Border x:Name="ThumbBorder"
                                                    Background="#606060"
                                                    CornerRadius="2"
                                                    Opacity="0.5"/>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="ThumbBorder" Property="Opacity" Value="0.7"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Thumb.Template>
                                </Thumb>
                            </Track.Thumb>
                        </Track>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Orientation" Value="Horizontal">
                            <Setter Property="Width" Value="Auto"/>
                            <Setter Property="MinWidth" Value="0"/>
                            <Setter Property="Height" Value="4"/>
                            <Setter Property="MinHeight" Value="4"/>
                            <Setter TargetName="PART_Track" Property="IsDirectionReversed" Value="false"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
